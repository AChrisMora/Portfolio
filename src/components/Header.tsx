import Nav from "./Nav";

const Header = () => {
  return (
    <header className="justify-content-center mx-auto text-center" style={{ width: '80%' }}>
      <Nav />
    </header>
  );
};

export default Header;
